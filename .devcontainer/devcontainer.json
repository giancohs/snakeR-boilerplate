// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/rocker-org/devcontainer-templates/tree/main/src/r-ver
{
	// Display and container name. "Clone in Container" has no local folder so we use a fixed name; replace with your repo name when using this as a template. For "Open Folder" → "Reopen in Container" you can use "${localWorkspaceFolderBasename}" for both if your client supports it.
	"name": "snakeR-boilerplate",
	// Base image; actual Ubuntu/R/Python versions are written to docs-and-references/installed-versions.md by install-dev-tools.sh
	"image": "ghcr.io/rocker-org/devcontainer/tidyverse:4.3",
	// Container name in Docker (avoids long/random names when using "Clone in Container"). Replace with your repo name when using this template for a new repo.
	"runArgs": ["--name", "snakeR-boilerplate"],

	"features": {
		"ghcr.io/rocker-org/devcontainer-features/quarto-cli:1.1.2": {},
		// ADDED: Lightweight virtual desktop for GUI apps like Chrome
		"ghcr.io/devcontainers/features/desktop-lite:1": {
			"webPort": "6080",
			"vncPort": "5901"
		}
		// currently have some issues to install r-packages. (31/10/2023)
		//"ghcr.io/rocker-org/devcontainer-features/r-packages:1.0.3": {
		//    "packages": "plotly,here,readxl,networkD3,leaflet,sf,spData,reactable"
		//}	
	},

	// ADDED: Forward the web port so you can view the desktop from your host browser
	"forwardPorts": [6080, 5901],

	// Runs when workspace is available. postCreateCommand often runs before the repo exists in "Clone in Container", so we use postStartCommand; the script is idempotent (runs once per container).
	"postStartCommand": "bash install-dev-tools.sh",

	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code/Cursor.
		"vscode": {
			// Add the IDs of extensions you want installed when the container is created.
			"extensions": ["ms-python.python", "ms-toolsai.jupyter"]
		}
	},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// should not use root, but for late: https://stackoverflow.com/questions/64191385/i-get-an-error-when-trying-to-run-as-a-remote-user-for-vs-code-development-conta
	"remoteUser": "root"
}